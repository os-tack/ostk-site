---
import FeaturePage from '../../layouts/FeaturePage.astro';

const relatedPages = [
  { title: "Multi-Agent Coordination", description: "Conflict resolution across agents", href: "/features/coordination/" },
  { title: "Audit Trail", description: "Every edit tracked and signed", href: "/features/audit-trail/" },
  { title: "Five Laws", description: "The design constraints", href: "/docs/five-laws/" },
];
---

<FeaturePage
  tagline="FEATURE // CORE_ARCHITECTURE"
  title="AI Agent File Write Tracking"
  description="Agents edit files with their normal tools. The OS intercepts every write, coordinates between agents, and maintains an audit trail. The agent never knows."
  relatedPages={relatedPages}
>
  <h2>The principle</h2>
  <p>
    AI agents already know how to write files. Claude uses <code>write_to_file</code>. GPT uses code interpreter. Every agent has a file-writing mechanism built in.
  </p>
  <p>
    ostk does not replace these tools. It does not ask agents to learn a new API, call a special endpoint, or use a custom SDK. Agents write files exactly the way they normally would. The OS intercepts those writes, handles coordination and tracking, and lets the write complete.
  </p>

  <h2>What happens when an agent writes a file</h2>
  <ol>
    <li>The agent issues a file write through its normal tool.</li>
    <li>The OS intercepts the write before it hits disk.</li>
    <li>The OS checks for conflicts with other agents on the same file.</li>
    <li>The OS records the write in the audit trail — who, what, when, and the full diff.</li>
    <li>The write completes. The file on disk matches what the agent intended.</li>
  </ol>

  <h2>Byte-for-byte passthrough</h2>
  <p>
    The OS never modifies the content of a write (outside of conflict resolution). If an agent writes 847 bytes to <code>src/index.js</code>, exactly 847 bytes land on disk. No comments injected, no watermarks, no reformatting.
  </p>
  <p>
    This matters because agents verify their own writes. Many agents read a file back after writing to confirm the edit took effect. If the OS modified the content, the agent would detect the discrepancy and enter a retry loop. Byte-for-byte passthrough means self-verification always succeeds.
  </p>

  <h2>How the interception actually works</h2>
  <p>
    Default interception is at the <strong>tool layer</strong>. Agents write through kernel primitives (<code>file:edit</code>, <code>file:write</code>, <code>file:create</code>) that apply policy, locking, and version tracking before bytes hit disk. No <code>LD_PRELOAD</code>, no <code>ptrace</code>, no <code>kqueue</code> observer. If a write went through the tool, the kernel saw it, tracked it, and appended it to the audit trail.
  </p>
  <p>
    The honest question: what if an agent bypasses the tool and writes directly through a shell? Two defenses handle that:
  </p>
  <ul>
    <li><strong>Process sandbox (always on).</strong> The <code>nono</code> subsystem enforces filesystem restrictions at the OS layer — Landlock on Linux, Seatbelt on macOS. Direct writes outside allowed paths physically fail at the syscall level, regardless of which tool the agent used. Source: <code>src/kernel/sandbox.rs</code>.</li>
    <li><strong>FUSE passthrough (optional).</strong> <code>ostk mount</code> (built with <code>--features fuse</code>) mounts a FUSE filesystem that mirrors the project tree and logs every write op. Off by default because FUSE adds dependencies; turned on when operators want every byte logged regardless of how it got written. Source: <code>src/fuse/passthrough.rs</code>.</li>
  </ul>
  <p>
    Tool layer covers the expected path (and gets you conflict resolution + audit). The sandbox covers the escape path (containment, not observability). FUSE is the observability upgrade for users who need both. No single layer is load-bearing on its own — and none of them require the agent to know they exist.
  </p>
</FeaturePage>
