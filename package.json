{
  "name": "ostk-site",
  "type": "module",
  "version": "3.0.0",
  "scripts": {
    "dev": "cd v2 && npm run dev",
    "build": "cd v2 && npm install && npm run build",
    "preview": "cd v2 && npm run preview"
  }
}
