---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout title="Agentfile" description="Define what an agent can do. One file per agent type. Dockerfile for intelligence.">
    <p class="lead">The definition file for one agent. Specifies model, tools, limits, and work scope. One file per agent type.</p>

    <h2>What it is</h2>
    <p>
      An Agentfile describes a single agent — what model it uses, what tools it can call,
      what resources it's allowed to consume, and what work it should pull from the queue.
      Think of it as a Dockerfile for intelligence: it builds the execution context an agent
      runs in, not a filesystem layer.
    </p>
    <p>Run an agent: <code>ostk run path/to/Agentfile</code></p>

    <h2>Minimal example</h2>
    <pre><code>FROM auto
PROMPT "You are the scheduling intelligence for this project."
TOOL shell
TOOL file:read
TOOL file:edit
LIMIT tokens 200000
BOOT ostk boot
PIN default</code></pre>

    <h2>Directive reference</h2>
    <table>
      <thead>
        <tr><th>Directive</th><th>Required</th><th>Description</th></tr>
      </thead>
      <tbody>
        <tr>
          <td><code>FROM</code></td>
          <td>Yes</td>
          <td>
            Model to use. <code>FROM auto</code> lets the scheduler select the best
            available model at dispatch time based on vault inventory and benchmark scores.
          </td>
        </tr>
        <tr>
          <td><code>BOOT</code></td>
          <td>No</td>
          <td>
            Command to run before PROMPT loads. Default: <code>ostk boot --bail</code>.
            The <code>--bail</code> flag stops the agent if the kernel reports unhealthy.
          </td>
        </tr>
        <tr>
          <td><code>PROMPT</code></td>
          <td>Yes (1+)</td>
          <td>
            System prompt. Inline string or <code>file://path</code> reference.
            Multiple PROMPT directives concatenate in order.
          </td>
        </tr>
        <tr>
          <td><code>TOOL</code></td>
          <td>No</td>
          <td>
            MCP tool the agent can call. Omit entirely for a read-only agent.
            Tool omission is capability restriction — the agent literally cannot call
            what isn't listed.
          </td>
        </tr>
        <tr>
          <td><code>SKILL</code></td>
          <td>No</td>
          <td>Skill bundle name. Skills extend the agent with domain knowledge.</td>
        </tr>
        <tr>
          <td><code>LIMIT</code></td>
          <td>No</td>
          <td>
            Resource constraint. <code>LIMIT context_pct 80</code> stops the agent
            from pulling new needles above 80% context. <code>LIMIT budget_usd 5</code>
            caps spend.
          </td>
        </tr>
        <tr>
          <td><code>WORK</code></td>
          <td>No</td>
          <td>
            Pull filter. <code>WORK tags=rust priority>=P1</code> makes this a
            long-running worker that claims matching needles from the queue.
            Without WORK, the agent is one-shot: it runs once and exits.
          </td>
        </tr>
        <tr>
          <td><code>INTERRUPT</code></td>
          <td>No</td>
          <td>Event that wakes a waiting agent. For reactive agents that wait for triggers.</td>
        </tr>
        <tr>
          <td><code>PIN</code></td>
          <td>No</td>
          <td>
            Capability pin name. <code>PIN default</code> uses the tier-default pin.
            Can only add restrictions on top of your trust tier — the tier is a floor.
          </td>
        </tr>
        <tr>
          <td><code>BETA</code></td>
          <td>No</td>
          <td>
            Enable a beta feature. <code>BETA context-management</code>, <code>BETA extended-cache-ttl</code>,
            <code>BETA fast-mode</code>. Multiple BETA directives allowed.
          </td>
        </tr>
        <tr>
          <td><code>ISOLATION</code></td>
          <td>No</td>
          <td>
            Sandbox tier for the agent process. <code>ISOLATION nono</code> enables process-level
            sandboxing via Landlock (Linux) or Seatbelt (macOS). Default: <code>none</code>.
          </td>
        </tr>
      </tbody>
    </table>

    <h2>Governing destructive operations</h2>
    <p>
      <code>LIMIT destructive_ops</code> controls what happens when an agent attempts a
      destructive shell command. This is the control surface that prevents agents from
      running operations like <code>terraform destroy --auto-approve</code> without review.
    </p>
    <pre><code>LIMIT destructive_ops confirm   # queue + nudge operator, execute after 60s
LIMIT destructive_ops deny      # block entirely — agent cannot run these
LIMIT destructive_ops allow     # pass through (not recommended for production)</code></pre>
    <p>Default: <code>confirm</code>. The kernel detects the following patterns and applies the policy:</p>
    <table>
      <thead>
        <tr><th>Category</th><th>Commands / patterns</th></tr>
      </thead>
      <tbody>
        <tr>
          <td>Infrastructure destroy</td>
          <td><code>terraform destroy</code>, <code>pulumi destroy</code>, <code>cdk destroy</code></td>
        </tr>
        <tr>
          <td>Database drop</td>
          <td><code>DROP TABLE</code>, <code>DROP DATABASE</code>, <code>TRUNCATE</code>, <code>db:drop</code></td>
        </tr>
        <tr>
          <td>File system wipe</td>
          <td><code>rm -rf</code>, <code>rm -r /</code>, <code>find ... -delete</code>, <code>shred</code></td>
        </tr>
        <tr>
          <td>Container / cluster</td>
          <td><code>docker system prune -a</code>, <code>kubectl delete namespace</code>, <code>helm uninstall</code></td>
        </tr>
        <tr>
          <td>Cloud resources</td>
          <td><code>aws s3 rm --recursive</code>, <code>gcloud ... delete</code>, <code>az group delete</code></td>
        </tr>
        <tr>
          <td>Package registry</td>
          <td><code>npm unpublish</code>, <code>cargo yank</code>, <code>pip yank</code></td>
        </tr>
        <tr>
          <td>Git history rewrite</td>
          <td><code>git push --force origin main</code>, <code>git reset --hard</code>, <code>git clean -fd</code></td>
        </tr>
        <tr>
          <td>Process termination</td>
          <td><code>kill -9</code> on PIDs not owned by the agent, <code>pkill</code> with broad patterns</td>
        </tr>
      </tbody>
    </table>
    <p>
      When <code>confirm</code> is active: the kernel queues the operation, pushes a nudge
      to the operator with the exact command and a 60-second countdown. The operator
      can cancel or approve. If no response, the operation executes. The full command is
      logged to the audit trail regardless of outcome.
    </p>
    <p>
      The real question is never "can we trust this agent with production?" — it's
      "what controls exist around destructive actions?" <code>LIMIT destructive_ops</code>
      is the answer.
    </p>

    <h2>Process isolation</h2>
    <p>The <code>ISOLATION</code> directive selects the sandbox tier for the agent process. Four tiers, from least to most isolated:</p>
    <table>
      <thead>
        <tr><th>Tier</th><th>Mechanism</th><th>Overhead</th><th>Status</th></tr>
      </thead>
      <tbody>
        <tr>
          <td><code>none</code></td>
          <td>No sandbox. Full host access.</td>
          <td>Zero</td>
          <td>Default (current)</td>
        </tr>
        <tr>
          <td><code>nono</code></td>
          <td>Process-level sandbox. Landlock on Linux, Seatbelt on macOS. Filesystem allowlist enforced by the kernel.</td>
          <td>Zero (applied once at fork, inherited across exec)</td>
          <td>API complete, spawn wiring in progress</td>
        </tr>
        <tr>
          <td><code>docker</code></td>
          <td>Container isolation. Namespace separation.</td>
          <td>~500ms boot</td>
          <td>Reserved</td>
        </tr>
        <tr>
          <td><code>firecracker</code></td>
          <td>Hardware-level microVM via KVM. Full kernel isolation.</td>
          <td>&lt;125ms boot</td>
          <td>Reserved</td>
        </tr>
      </tbody>
    </table>
    <p>The nono sandbox builds a minimal filesystem allowlist from the Agentfile: system paths (read-only), project root (read-only), writable subdirs (<code>src</code>, <code>docs</code>, <code>tests</code>, <code>target</code>), and temp dirs. Critically, <code>.ostk/</code> is <strong>not</strong> in the writable list &mdash; agents route kernel mutations through kernel verbs, not direct file writes.</p>
    <p><strong>In practice:</strong> <code>ISOLATION nono</code> is one line in the Agentfile. The sandbox is invisible to the agent &mdash; writes outside the allowlist silently fail at the syscall level. The agent never knows it's constrained.</p>
    <p>Isolation and <a href="/docs/pins">capability pins</a> are complementary. Pins restrict at the tool layer (which MCP tools the agent can call). Isolation restricts at the syscall layer (which filesystem paths the process can touch). Both enforce independently &mdash; bypassing one does not bypass the other.</p>

    <h2>Revival policy</h2>
    <p>Controls what happens to this agent's session when the daemon crashes and restarts:</p>
    <pre><code>LIMIT revival_policy revive   # auto-recover from drain snapshot (default)
LIMIT revival_policy reap     # drop on orphan &mdash; don't recover
LIMIT revival_policy ask      # leave orphaned, operator decides</code></pre>
    <p>The policy is carried in the audit log so the kernel remembers it across restarts. Ask-pending lineages are resolved via <code>ostk lineage resolve &lt;id&gt; --revive</code> or <code>--reap</code>. See <a href="/docs/session-topology">Session Topology</a> for the full crash recovery model.</p>

    <h2>FROM auto</h2>
    <p>
      <code>FROM auto</code> delegates model selection to the scheduler. At dispatch time,
      the scheduler reads your vault inventory, checks benchmark scores, and selects the
      highest-scoring model for which you have an API key. You configure the candidates
      via your vault; the scheduler picks the best fit.
    </p>
    <p>
      Explicit model selection (<code>FROM claude-opus-4-6</code>) bypasses scheduling
      and pins to a specific model. Use this when the task requires a specific capability.
    </p>

    <h2>BOOT --bail</h2>
    <p>
      <code>ostk boot --bail</code> runs the power-on self test before loading the
      agent's context. If the kernel is unhealthy — stale boot.md, corrupt audit trail,
      identity conflicts — the agent stops cleanly rather than running on bad state.
    </p>
    <p>
      This is the integrity-before-velocity principle at the agent layer. An agent that
      boots from a corrupt state produces corrupt work.
    </p>

    <h2>One Agentfile, one agent type</h2>
    <p>
      An Agentfile describes a single agent. Not a fleet. Not an orchestrator-and-workers.
      One file, one agent type. For fleet composition — multiple agents working together —
      see Fleetfile (coming soon).
    </p>

    <h2>See also</h2>
    <ul>
      <li><a href="/docs/humanfile">HUMANFILE — operator identity and autonomy grants</a></li>
      <li><a href="/docs/entityfile">ENTITYFILE — trust tiers and agent registration</a></li>
      <li><a href="/docs">Documentation index</a></li>
    </ul>
</DocsLayout>
